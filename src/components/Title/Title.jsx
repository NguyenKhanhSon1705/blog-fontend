// eslint-disable-next-line no-unused-vars
import React from 'react';

// eslint-disable-next-line react/prop-types
const Title = ({title}) => {
    return (
        <>
            <h1 className="font-medium text-black text-2xl">{title}</h1>
        </>
    );
};

export default Title;